{
  "name": "common",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tsc": "tsc --noEmit",
    "build": "yarn tsc"
  },
  "dependencies": {
    "firebase": "^7.21.0",
    "immutable": "^4.0.0-rc.12"
  }
}
